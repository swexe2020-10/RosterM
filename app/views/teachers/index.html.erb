<h1>先生一覧</h1>

<% if flash[:info] %>
  <font color="green"><%= flash[:info] %></font>
<% end %>

<% @teachers.each do |e| %>
    <p>
        <%=e.uid%>
        <%= link_to '削除', teacher_path(e), method: :delete %>
    </p>
<% end %>


<hr>
<p>
<%= link_to '新規登録' , new_teacher_path %>
</p>